services:
  ncache:
    build: .
    container_name: ncache
    env_file:
      - .env
    restart: always
    deploy:
      resources:
        limits:
          memory: "200M"
    networks:
      - formstr

networks:
  formstr:
    external: true
